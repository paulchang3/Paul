# Windows 實機測試指南（260614 三大功能）

雲端只能驗證後端邏輯與通訊協定；GUI、跨螢幕覆蓋層、真實滑鼠這三件事
**一定要在你的 Windows 上跑**。以下是步驟。

## 0. 取得這個版本

把這個分支 `multi-mobile-qr-controller-260614` 的檔案下載／pull 到你的
`Multi-Mobile QR Controller project` 資料夾（覆蓋舊檔）。

## 1. 啟動

雙擊 `deploy.bat`（會自動建 venv、裝套件、開防火牆、啟動），或手動：

```bat
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python main.py
```

啟動後桌面會出現深色 GUI，左側有 QR Code 與新的「**控制**」區塊。

## 2. 連手機

手機連到**同一個 Wi-Fi**，掃描 GUI 上的 QR Code（或手動開 `http://<電腦IP>:8000`）。
連上後手機會顯示「方向鍵／搖桿／拖曳／滑鼠」四個模式，桌面小地圖出現對應圓圈。

## 3. 功能 1：跨螢幕最上層圓圈

- 在 GUI「控制」區按 **「螢幕覆蓋層（最上層）」** → 變綠色「開」。
- 圓圈會疊在**整個桌面最上層**；把視窗拉到延伸螢幕也看得到圓圈跑過去。
- 用手機（拖曳或滑鼠模式）移動，圓圈可到任一螢幕的任一角落，無邊界。
- 覆蓋層是**滑鼠穿透**的，不會擋住你點桌面。再按一次即關閉。

## 4. 功能 2：第一台手機 = 滑鼠

> ⚠️ 這會讓第一台手機接管你的游標。先確定鍵盤可操作（Win 鍵、Alt+Tab 仍可用），
> 真的失控時回 GUI 取消勾選即可。

- 第一台連線的手機（手機 A）頂端會顯示「🖱️ 你是第一台手機 ＝ 滑鼠」，並自動切到**滑鼠**模式。
- 在 GUI「控制」區按 **「第一台手機 = 滑鼠」** → 開。
- 在手機的觸控板上：
  - **滑動** → 游標跟著移動（可跨到延伸螢幕）。
  - **單擊** → 選擇（左鍵單擊）。
  - **雙擊** → 啟動（左鍵雙擊，例如開啟圖示）。
  - **雙指上下滑動** → 滾輪上下捲動。
- 第一台手機關掉後，下一台會自動變成第一台（頂端橫幅會跳到那支手機）。

## 5. 功能 3：上傳照片當圓圈圖案

- 手機按 **「上傳照片（圓圈圖案）」** → 選一張相片。
- 前端會壓成 256×256 上傳；成功後小地圖、覆蓋層、`/screen` 三處的圓圈都會變成你的照片（裁成圓形）。
- 手機斷線後該照片自動清除。

## 6. 疑難排解

- **手機連不上**：多半是防火牆。用系統管理員身分跑一次 `deploy.bat`，或手動開放 TCP 8000。
- **覆蓋層沒出現**：確認 GUI 沒有縮到最小化；覆蓋層只在「開」時顯示。多螢幕請確認延伸（非鏡像）。
- **滑鼠沒反應**：確認 GUI 已勾「第一台手機=滑鼠」，且操作的是**第一台**手機（看頂端橫幅）。
- **照片沒變**：確認手機有顯示「✓ 已更新圓圈圖案」；過大或非圖片會被拒絕。
- 後端自我測試（不需 GUI）：`python -m pytest` 應全綠。
