# Multi-Mobile QR Controller project_260614（上傳區）

這個資料夾是給你重新上傳本機程式碼用的：
`E:\113_製作\700_Python\011_開發中_2603\Multi-Mobile QR Controller project`

> 為什麼是 `Paul` 倉庫裡的資料夾，而不是新倉庫？
> 因為這個雲端 session 的讀取權限只鎖定 `paulchang3/Paul`，
> 新開的獨立倉庫我反而讀不到。放在這裡我才能馬上接手。

## 怎麼上傳（用瀏覽器，不用 git）

1. 點這個連結，會**直接進到上傳畫面、目標就是這個資料夾**：
   <https://github.com/paulchang3/Paul/upload/claude/wonderful-clarke-ekgo50/Multi-Mobile-QR-Controller-project_260614>
2. 把整個專案的檔案**拖進去**（`.py` / `.html` / `.js` / `.css` / `requirements.txt` 等程式碼檔；
   先**不用**傳 `venv/`、大圖、影片這類大檔）。
3. 拉到頁面**最下面**，按綠色的 **Commit changes** 按鈕。
   ← 上次檔案沒進來，最可能就是漏了這一步。
4. 回來跟我說「**傳好了**」，我就開始：讀程式 → 整理 SRS → 加 headless 自我檢測 → 實作三個功能。

## 三個要實作的功能（提醒，等程式到了再做）

1. 每台手機在整個桌面（含延伸螢幕）最上層自由移動的圓圈。
2. 第一台手機 = 滑鼠：單擊選取、雙擊啟動、雙指上下滑動 = 滾輪。
3. 手機 UI 可上傳照片，當作圓圈顯示的圖。

上傳成功後這份說明檔可以留著，也可以叫我清掉。
